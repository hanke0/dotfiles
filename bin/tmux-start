#!/usr/bin/env bash

if [[ -z $1 ]]; then
  session_name=$(whoami)
else
  session_name=$1
fi

if tmux ls 2>/dev/null | cut -d: -f1 | grep ${session_name}; then
  tmux at -t ${session_name}
else
  tmux -2 new -s ${session_name}
fi
